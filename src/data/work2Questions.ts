import type { Question } from '../lib/scoring';

// Placeholder: 20 questions to be provided. The structure below illustrates the schema.
// Each option's `type` is one of: 'attacker' | 'guardian' | 'analyst' | 'booster'.
export const work2Questions: Question[] = [];
